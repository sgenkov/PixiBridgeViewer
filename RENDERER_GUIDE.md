# 🎨 Ръководство за Рендерери

Този проект поддържа два различни рендерера за визуализация на данните от WebSocket сървъра.

## Какво е Рендерер?

Рендерерът е технологията, която се използва за визуализиране на данните в браузъра. Проектът предлага два варианта:

### 🎮 PixiJS Рендерер (по подразбиране)

**Технология:** PixiJS - 2D WebGL библиотека за high-performance графика

**Предимства:**
- ⚡ Много бързо рендериране (hardware-accelerated WebGL)
- 🎯 Отлично за сложни анимации и визуални ефекти
- 📊 Ниско CPU натоварване
- 🎮 Подходящ за игрови визуализации

**Недостатъци:**
- 🔧 По-сложен за модифициране
- 📚 Изисква познания за PixiJS API
- 🎨 По-труден за стилизиране

**Файлове:**
- `client/app-pixi.ts` - основна логика
- `client/config.ts` - стилове и конфигурация

### ⚛️ React Рендерер

**Технология:** React - модерна библиотека за изграждане на потребителски интерфейси

**Предимства:**
- 🔧 Много лесен за модифициране
- 📦 Компонентна архитектура
- 🎨 Лесно стилизиране с CSS/inline styles
- 💡 По-добра четимост на кода
- 🔄 React екосистема и инструменти

**Недостатъци:**
- 🐌 Малко по-бавен от PixiJS (но достатъчен за повечето случаи)
- 📈 Малко по-високо CPU натоварване

**Файлове:**
- `client/app-react.tsx` - React компонент
- `client/index-react.tsx` - React entry point

## 🔄 Превключване между Рендерери

### Бърз метод (препоръчително)

```bash
# Превключване към PixiJS
npm run switch:pixi

# Превключване към React
npm run switch:react
```

След превключване, рестартирайте dev сървъра или rebuild-нете:

```bash
npm run dev   # За development
# или
npm run build # За production build
```

### Ръчен метод

Отворете `client/renderer.config.ts` и променете:

```typescript
export const RENDERER_TYPE: RendererType = 'pixi'; // или 'react'
```

## 🏗️ Архитектура

```
client/
├── app.ts                  # Главен entry point (избира рендерер)
├── renderer.config.ts      # Конфигурация на рендерера
│
├── app-pixi.ts            # PixiJS имплементация
├── config.ts              # PixiJS стилове
│
├── app-react.tsx          # React компонент
├── index-react.tsx        # React entry point
│
├── utils.ts               # Споделени utility функции
└── interfaces/
    └── Interfaces.ts      # TypeScript интерфейси
```

## 🎯 Кой рендерер да изберете?

**Изберете PixiJS ако:**
- Имате нужда от високопроизводителни анимации
- Работите с много визуални елементи
- Искате hardware-accelerated графика
- Проектът е за игри или симулации

**Изберете React ако:**
- Искате лесна поддръжка и модификации
- Предпочитате React екосистемата
- Не ви трябват сложни анимации
- Искате бързо да добавяте нови функционалности

## 📝 Пример: Добавяне на нова функция

### В PixiJS:
```typescript
// app-pixi.ts
const newText = new PIXI.Text({ text: 'Hello', style: myStyle });
contentContainer.addChild(newText);
```

### В React:
```tsx
// app-react.tsx
<div style={styles.newElement}>
  Hello
</div>
```

## 🔧 Персонализиране

### PixiJS Стилове:
Редактирайте `client/config.ts`:
```typescript
const valueTextStyle = new PIXI.TextStyle({
  fill: '#c7d2f0',
  fontSize: 36,
  fontFamily: 'Arial'
});
```

### React Стилове:
Редактирайте `client/app-react.tsx`:
```typescript
const styles = {
  value: {
    fontSize: '36px',
    color: '#c7d2f0',
  }
};
```

## 🐛 Debugging

И двата рендерера поддържат натискане на клавиш `M` за отпечатване на текущото състояние в конзолата:

```javascript
// Натиснете 'M' в браузъра
console.log('MAP: ', payloadMap);
```

## 📦 Зависимости

### PixiJS:
- `pixi.js` - основната библиотека
- `@types/pixi.js` - TypeScript типове

### React:
- `react` - React библиотека
- `react-dom` - React DOM рендерер
- `@types/react` - TypeScript типове
- `@types/react-dom` - TypeScript типове за DOM

Всички зависимости се инсталират автоматично с `npm install`.

# 🧪 Тестване на Рендерерите

Това ръководство ще ви помогне да тествате и двата рендерера.

## Стъпки за тестване

### 1. Тест на PixiJS Рендерер

```bash
# Превключване към PixiJS
npm run switch:pixi

# Стартиране на dev сървъра
npm run dev
```

Браузърът ще се отвори автоматично на `http://localhost:8787`

**Очаквани резултати:**
- Заглавие: "**Pixi Bridge Viewer**"
- Тъмен фон (#0b0f1a)
- Текст със стил подобен на игров UI
- Плавно рендериране

### 2. Изпращане на тестови данни

В нов терминал:

```bash
npm run send
```

**Очаквани резултати:**
- Статус се променя на "Status: connected" (зелен)
- Показват се данните от sender-example.js
- Примерен изход:
  ```
  mode: MEGA_FREE_SPINS
  multiplier: 14
  reel: 2
  symbol: scatter
  ```

### 3. Тест на React Рендерер

Спрете dev сървъра (Ctrl+C), след това:

```bash
# Превключване към React
npm run switch:react

# Стартиране на dev сървъра
npm run dev
```

**Очаквани резултати:**
- Заглавие: "**React Bridge Viewer**"
- Подобен изглед, но React-базиран
- Статус индикатор
- Същите данни се визуализират коректно

### 4. Изпращане на тестови данни (отново)

```bash
npm run send
```

Резултатът трябва да е идентичен на PixiJS версията.

## 🔍 Проверка на различията

### Visual Inspection

| Характеристика | PixiJS | React |
|----------------|--------|-------|
| Заглавие | "Pixi Bridge Viewer" | "React Bridge Viewer" |
| Технология | WebGL Canvas | HTML/CSS |
| DevTools | Има `__PIXI_APP__` в window | Има React DevTools |

### Browser DevTools

#### PixiJS:
Отворете конзолата и напишете:
```javascript
window.__PIXI_APP__
```
Трябва да видите PixiJS Application обект.

#### React:
Инсталирайте React DevTools разширение и проверете дали виждате React компонентна йерархия.

## 🎯 Test Checklist

### Функционалност (и двата рендерера)

- [ ] WebSocket връзка се установява успешно
- [ ] Статус индикаторът показва "connected" със зелен цвят
- [ ] Получени данни се визуализират коректно
- [ ] Натискане на клавиш `M` отпечатва payload в конзолата
- [ ] Refresh на payload работи (`refresh: true`)
- [ ] Custom text styles се прилагат (ако има)

### PixiJS Специфично

- [ ] Canvas елемент се създава в DOM
- [ ] Текстът е с anti-aliasing
- [ ] Scaling работи при resize на прозореца
- [ ] `__PIXI_APP__` е достъпен в window

### React Специфично

- [ ] HTML елементи се рендерират
- [ ] Inline стилове се прилагат
- [ ] React component state се обновява
- [ ] React DevTools работи (ако е инсталиран)

## 🐛 Debugging

### PixiJS не се зарежда:

1. Проверете дали `pixi.js` е в `node_modules/pixi.js/dist/`
2. Отворете Network таб и проверете дали `pixi.js` се зарежда
3. Проверете конзолата за грешки

### React не се зарежда:

1. Проверете дали React зависимостите са инсталирани: `npm list react`
2. Проверете дали `react.development.js` и `react-dom.development.js` са в node_modules
3. Проверете Browser конзолата за грешки

### WebSocket не се свързва:

1. Уверете се, че сървърът е стартиран: `node server/index.js`
2. Проверете дали порт 8787 е свободен
3. Проверете Network таб за WebSocket връзката

## 📊 Performance Comparison

За да сравните производителността:

### Стъпка 1: Подгответе тестови данни

Модифицирайте `server/sender-example.js` да изпраща повече данни:

```javascript
// Изпратете 50 реда данни вместо 4
const payload = [];
for (let i = 0; i < 50; i++) {
  payload.push({
    name: `value${i}`,
    value: Math.random() * 100
  });
}
```

### Стъпка 2: Измерете Performance

1. Отворете Chrome DevTools > Performance tab
2. Стартирайте запис
3. Изпратете тестови данни: `npm run send`
4. Спрете записа

Сравнете резултатите за PixiJS и React.

**Очаквани резултати:**
- PixiJS: По-ниско CPU usage, по-бързо рендериране
- React: Малко по-високо CPU usage, но все още бързо

## ✅ Success Criteria

Тестът е успешен когато:

1. ✅ И двата рендерера се зареждат без грешки
2. ✅ WebSocket връзката работи и с двата
3. ✅ Данните се визуализират идентично
4. ✅ Превключването между рендерери е лесно и работи
5. ✅ Няма TypeScript или JavaScript грешки в конзолата

## 📝 Забележки

- За production use, изберете един рендерер и премахнете другия за по-малък bundle size
- PixiJS е препоръчителен за high-performance scenarios
- React е препоръчителен за бързо development и лесна поддръжка
